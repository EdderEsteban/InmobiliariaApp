<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- ViewPager para mostrar fotos -->
    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/viewPagerFotos"
        android:layout_width="0dp"
        android:layout_height="250dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginBottom="16dp" />

    <!-- Código del inmueble -->
    <TextView
        android:id="@+id/tv1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Código:"
        android:textSize="18sp"
        android:textColor="@color/violet"
        app:layout_constraintTop_toBottomOf="@id/viewPagerFotos"
        app:layout_constraintStart_toStartOf="parent" />

    <TextView
        android:id="@+id/tvCodigoDetalle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="123456"
        android:textSize="18sp"
        app:layout_constraintTop_toTopOf="@id/tv1"
        app:layout_constraintStart_toEndOf="@id/tv1"
        app:layout_constraintBaseline_toBaselineOf="@id/tv1"
        android:layout_marginStart="8dp" />

    <!-- Dirección del inmueble -->
    <TextView
        android:id="@+id/tv2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Dirección:"
        android:textSize="18sp"
        android:textColor="@color/violet"
        app:layout_constraintTop_toBottomOf="@id/tv1"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="8dp" />

    <TextView
        android:id="@+id/tvDireccionDetalle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="123 Main Street"
        android:textSize="18sp"
        app:layout_constraintTop_toTopOf="@id/tv2"
        app:layout_constraintStart_toEndOf="@id/tv2"
        app:layout_constraintBaseline_toBaselineOf="@id/tv2"
        android:layout_marginStart="8dp" />

    <!-- Tipo del inmueble -->
    <TextView
        android:id="@+id/tv3"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Tipo:"
        android:textSize="18sp"
        android:textColor="@color/violet"
        app:layout_constraintTop_toBottomOf="@id/tv2"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="8dp" />

    <TextView
        android:id="@+id/tvTipoDetalle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Residencial"
        android:textSize="18sp"
        app:layout_constraintTop_toTopOf="@id/tv3"
        app:layout_constraintStart_toEndOf="@id/tv3"
        app:layout_constraintBaseline_toBaselineOf="@id/tv3"
        android:layout_marginStart="8dp" />

    <!-- Uso del inmueble -->
    <TextView
        android:id="@+id/tv4"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Uso:"
        android:textSize="18sp"
        android:textColor="@color/violet"
        app:layout_constraintTop_toBottomOf="@id/tv3"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="8dp" />

    <TextView
        android:id="@+id/tvUsoDetalle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Comercial"
        android:textSize="18sp"
        app:layout_constraintTop_toTopOf="@id/tv4"
        app:layout_constraintStart_toEndOf="@id/tv4"
        app:layout_constraintBaseline_toBaselineOf="@id/tv4"
        android:layout_marginStart="8dp" />

    <!-- Cantidad de ambientes -->
    <TextView
        android:id="@+id/tv5"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Ambientes:"
        android:textSize="18sp"
        android:textColor="@color/violet"
        app:layout_constraintTop_toBottomOf="@id/tv4"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="8dp" />

    <TextView
        android:id="@+id/tvAmbientesDetalle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="3"
        android:textSize="18sp"
        app:layout_constraintTop_toTopOf="@id/tv5"
        app:layout_constraintStart_toEndOf="@id/tv5"
        app:layout_constraintBaseline_toBaselineOf="@id/tv5"
        android:layout_marginStart="8dp" />

    <!-- Precio del inmueble -->
    <TextView
        android:id="@+id/tvPrecioDetalle2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Precio:"
        android:textSize="18sp"
        android:textColor="@color/violet"
        app:layout_constraintTop_toBottomOf="@id/tv5"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="8dp" />

    <TextView
        android:id="@+id/tvPrecioDetalle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="$5000"
        android:textSize="18sp"
        app:layout_constraintTop_toTopOf="@id/tvPrecioDetalle2"
        app:layout_constraintStart_toEndOf="@id/tvPrecioDetalle2"
        app:layout_constraintBaseline_toBaselineOf="@id/tvPrecioDetalle2"
        android:layout_marginStart="8dp" />

    <!-- Estado del inmueble -->
    <TextView
        android:id="@+id/tvEstadoDetalle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Estado:"
        android:textColor="@color/violet"
        android:textSize="18sp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tvPrecioDetalle2"
        android:layout_marginTop="16dp" />

    <!-- Switch Activo/Inactivo alineado con Estado -->
    <Switch
        android:id="@+id/swActivo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:checked="true"
        android:text="Activo"
        android:textSize="18sp"
        android:thumbTint="@color/switch_thumb_color"
        android:trackTint="@color/switch_track_color"
        app:layout_constraintTop_toTopOf="@id/tvEstadoDetalle"
        app:layout_constraintStart_toEndOf="@id/tvEstadoDetalle"
        android:layout_marginStart="16dp" />

    <!-- Botón para actualizar el estado -->
    <Button
        android:id="@+id/btnActualizarInmueble"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Actualizar Estado"
        app:layout_constraintTop_toBottomOf="@id/swActivo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="24dp" />

    <!-- Botón para agregar fotos -->
    <Button
        android:id="@+id/btnAddFotos"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="161dp"
        android:layout_marginTop="38dp"
        android:layout_marginEnd="144dp"
        android:layout_marginBottom="64dp"
        android:text="Agregar Fotos"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/btnActualizarInmueble" />

</androidx.constraintlayout.widget.ConstraintLayout>
